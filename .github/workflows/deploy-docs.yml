name: deploy-docs
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "*.*.*"

jobs:
  deploy:
    uses: Ascertia-Integrations/docusaurus-github-pages-poc/.github/workflows/reusable-deploy-docs.yml@main
    secrets: inherit
    with:
      # Use `npm ci` once package-lock.json is stabilized for this repo.
      install_command: npm install
    permissions:
      contents: write
      pages: write
      id-token: write
      packages: read
